file_generator is a super project to generate a lot of files with different
extensions. For now it's able to generate as much as you want files and you can choose the file name 
and the extention name.

